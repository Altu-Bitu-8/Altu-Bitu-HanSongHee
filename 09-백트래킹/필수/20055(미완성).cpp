#include <iostream>
#include <vector>
using namespace std;
int N, K;
vector<int> robot;
vector<bool> location;
int main() {
    //입력
    cin >> N >> K;
    roboot.resize(2 * N);
    location.resize(N, false);
    for (int i = 0; i < 2 * N; i++) {
        cin >> robot[i];
    }
    int step = 0;
    //연산
    //출력
    cout << step;
    return 0;
}